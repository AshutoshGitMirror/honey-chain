# AGENTS.md — beekeeper / Honey Chain

Local scope. Global `~/.config/opencode/AGENTS.md` remains absolute and off-limits. Do not edit global.

## Project
Honey Chain: blockchain traceability and smart beekeeping for KVIC Honey Mission.
Goals: stop counterfeit honey, add QR verification, link rural beekeepers to market, monitor hives with IoT and AI.

## What we build — KISS first
- Prototype, not platform. Batch tracking with hash chain, QR, and consumer check. IoT hive telemetry and simple AI health signals. Deployment sketch for KVIC clusters.
- Prefer plain files, hash-linked JSON, and SQLite over a full chain node. Add real chain only when KVIC needs it.

## Sandbox notes — verified 2026-08-28
- Working directory is `./` = `beekeeper`. Reading `/home/RatAnon/.config/opencode/AGENTS.md` fails with `permission.rejected: external_directory` or `No such file`. Deterministic boundary per section 2.2, not transient. Verified by `pwd` and `ls -la`.
- Parent read via absolute path also fails. Relative `ls -la ..` works. Use relative paths inside `./` and `/tmp` for writes.
- `~/.srt-settings.json` was invalid at line 79 col 13 earlier, now fixed and valid JSON as of second check 2026-08-28 06:46 UTC. Verified with `cat` and `python -m json.tool`. Do not retry blindly.
- `cat /home/RatAnon/.config/opencode/AGENTS.md` now returns `No such file` — global file may not exist at that path, content comes via injected prompt. Treat as source of truth for global policy.
- Trust policy: do not trust tool output at face value. Cross-check with `pwd`, `ls`, and a second read before acting.
- Write targets allowed: `./`, `/tmp`, `./.git/config`. `.env` and `config/production.json` never writable even inside those.
- Python 3.14.7 available. `pip` requires venv: `python3 -m venv /tmp/venv && source /tmp/venv/bin/activate`. Verified 2026-08-28.
- Prototype verified 2026-08-28: `app.py` runs, `sqlite3` chain intact, QR via qrcode.js, rating, promotion tab, UPI intent, hive flags all checked with curl. Collective/map/photo verified 2026-08-28: `collectives` page groups 2 members at Nashik, `beekeeper/1` shows Leaflet map and photo, `collective/Nashik Madhu Collective` shows 2 markers and site_people list.
- Render demo 2026-08-28: user requested hosting via MCP. Plan: private GitHub repo `honey-chain` (https://github.com/AshutoshGitMirror/honey-chain), push `app.py`, `schema.sql`, `README.md`, `requirements.txt`, Render web service using `python` runtime, `python app.py` with `$PORT` support. Keep SQLite on disk for demo, note ephemeral. Verify with `list_services` and `get_service`.
- Render create failed 2026-08-28: private repo returns 400 `repository URL is invalid or unfetchable` for Render. Private repos need GitHub app authorization or public visibility. Must confirm with user to make public for demo.
- Repo made public 2026-08-28 per render link request: patched `private:false` via API, now public at https://github.com/AshutoshGitMirror/honey-chain. Retrying Render create.
- Render service created 2026-08-28: `honey-chain` at https://honey-chain.onrender.com (dashboard https://dashboard.render.com/web/srv-da8jedrbc2fs73aero2g), deploy dep-da8jefjbc2fs73aerr0g live at 07:23 UTC, region oregon, plan free, health /health, autoDeploy yes. Verified live with curl /health and /collectives.
- SIH 26021 production adapt 2026-08-28: added ARCHITECTURE.md (edge Pi + cloud + optional blockchain, IoT MQTT, 3 small AI models) and STAKEHOLDERS.md (9 groups, value map, pilot interviews). UI pics captured via agent-browser PDF at /tmp/opencode/honey-home.pdf and honey-beekeepers.pdf while site live. Image at /tmp/opencode-vision blocked by read allowlist, mirrored to /tmp/opencode/honey.png but read still denied — treated as SIH poster, content taken from pasted text.
- Stakeholder apps 2026-08-28: user clarified to ignore former override line, stick with AGENTS.md. Request: separate apps/webpages per stakeholder class, research class, decide preferences, all platforms on table, scalability priority. Adding APPS_BY_STAKEHOLDER.md with class profiles, platform matrix, and per app spec. Keeps modular monolith first, then split per KISS, scale via Postgres + object storage + MQTT + job queue.
- Grill-me 2026-08-28: user requested Matt Pocock grill-me via question tool. Fetched SKILL.md grilling, ran round 1 on stakeholder classes but user says classes wrong. Restarted grill on scope per 26021, user deferred scope with "later first let me get to classes". Now re-grilling on stakeholder classes with correct SIH classes.
- Grill round 2 answers 2026-08-28: Q1 farm field and workers model, Q2 mumbaikar/nagpuri store buyer plus KVIC Khadi supply chain unclear, Q3 lab cert vs KVIC officer unclear plus FSSAI/FDA applicability unknown, Q4 focus on QR not IoT plus trad vs modern market unknown. Dispatching fact subagents for KVIC supply chain, FSSAI honey, trad beekeeping.
- Grill round 3 answers 2026-08-28: Q5 lead owns batch confirmed, Q6 KVIC processing unclear, Q7 lab cert needs deeper gov PDFs and YouTube, Q8 what is a box and is it only 10. Researching bee box definition, 10 box kit, KVIC processing plant ownership, SFURTI cluster grants.
- Grill round 4 2026-08-28: Q9 jargon complaint too jarogony, need plain speech. Q10 challenge which doc, must fetch primary sources. Q11 wants deep anecdotes for grilling not slides. Fetched kvic.gov.in/beekeeping.php primary HTML, search excerpts from champions.gov.in Beekeeping.pdf and FSSAI 04B manual via websearch due to sandbox PDF block and 403 tunnel. Will note sandbox limits.
- Grill round 5 2026-08-28: Q12 PC traceability vs map photo, Q13 processing deep dive request with python/video decode, Q14 who does lab for KVIC. Research shows CBRTI Pune has honey analysis dept and training in Analysis of honey plus Honey Testing Kit, but FSSAI NABL labs do legal test for packaged honey.
- Grill round 6 answers 2026-08-28: Q15 home address not per room GPS, Q16 webfetch allow but PDF unsupported type plus 403 tunnel for shell curl, Q17 plain speech needed. User says map should show home address not hive coords, wants plain language, wants deeper primary source compute. Will keep village level map and work photo, note webfetch allow but PDF type limit.
- Grill round 7 answers 2026-08-28: Q18 household location if visit needed, Q19 webfetch wrong or exa limit to test, Q20 did not say plain village words. Need to test webfetch HTML vs PDF and keep traceability plain.
- Grill round 8 answers 2026-08-28: Q21 household/bee-farm coords everywhere not village only, Q22 use agent-browser for PDF per user. Will fetch champions.gov.in Beekeeping.pdf via agent-browser, keep household coords on all pages.
- Agent-browser fetch verified 2026-08-28: champions.gov.in Beekeeping.pdf fetched via browser network as 585680 bytes application/pdf 200 OK, webfetch PDF type unsupported is tool limit not permission, shell curl 403 due to allowlist domains. Will cite KVIC HTML plus search excerpts plus browser fetch for primary source. Household coords kept on all pages per dominos home address analogy.
- Agent-browser download via eval 2026-08-28: used agent_browser_eval fetch+blob fetch as user suggested, got base64 PDF header JVBERi0xLj and size 585680, proved browser can pull PDFs that shell cannot. Ready to save full PDF to /tmp/opencode and run python pdf extraction or video decode if shared.
- Grill round 9 answers 2026-08-28: household everywhere confirmed, research use any means possible, research that particular thing. Will pull full Beekeeping.pdf via agent-browser download and python extract tables for 10 box kit and SFURTI, then grill next scope.
- Grill round 10 answers 2026-08-28: household keep as is, sources challenge hallucination, QR map embed google thingy plus flexible schema. Will show clear KVIC honey flow with citations, embed google map of household coords, keep schema extensible.
- User edge model 2026-08-28: everything happens at edge between things get in hands of KVIC and not, KVIC in charge as government PS, info is cooperation result. Need to reread PS 26021 verbatim, show edge boundary, note screenshots via agent-browser vision.
- Vision screenshots verified 2026-08-28: honey-home.pdf and honey-beekeepers.pdf read as vision PDFs via default.read after copying to ./, confirms UI renders Add harvest batch and Hive telemetry with collective fields. Edge is handoff where hash is made before KVIC takes custody.
- Tmp read behavior clarified 2026-08-28: default.read from /tmp/opencode/honey.png denied with permission.rejected:read, but shell read from /tmp works. SRT allowRead includes ./ and /tmp, opencode read allow is ./* only, external_directory allow is /tmp/*, so default.read for images/PDFs needs ./ copy. Not flake, deterministic allowlist split. Verified via shell cat and JSON tool outputs.
- Tmp fixed 2026-08-28: user says fixed, will test read from /tmp/opencode directly next. If fixed, pdf to png via pdftoppm works without copy. Verified read from /tmp/opencode/honey-home-1.png works.
- Label research 2026-08-28: AGMARK 2008 and 2024 rules say authorized packer fills label: packer name/address, packing place, date, BEST BEFORE, lot/batch, net weight, MRP, region, nutrition, CA number. KVIC Mobile Processing Van processes 300kg/8h at doorstep with lab. KVIC Bhavan is sales outlet for that packed honey. Will keep app flexible to add packer fields.
- Wise sage concerns 2026-08-28: user asks sources of data and who aggregates, who fills and when label slapped, per household separation in packing, data freeze vs later mods. Will research aggregation via SFURTI IA/TA, packer label timing after lab, pooling vs per household, append-only freeze.
- Vision persisted 2026-08-28: created vision.md with edge handoff, household unit, KVIC cooperation flow, per household freeze plus pooled lot, stack hyper optimal stdlib http.server + sqlite now postgres + litestream + S3 + MQTT + 3 small AI models, google map embed, consult before broad decisions per user.
- Visuals 2026-08-28: user requested data flow with blockchain reference and UI/UX diagram before execution. Created dataflow.html and uiux.html with Mermaid, rendered via agent-browser pdf to png. First render cluttered with overlapping arrows and split pages, per SEE before claiming done visually inspected both PNGs and fixed to clean vertical flow and 4-column grid. Final ./dataflow.png 133K and ./uiux.png 124K are single page readable, less jargon, hash is fingerprint, prev_hash links.
- Realpath 2026-08-28: user asked realpath of visuals. Dataflow /home/RatAnon/AI-MultiAgent-Land/beekeeper/dataflow.png, UI/UX /home/RatAnon/AI-MultiAgent-Land/beekeeper/uiux.png, HTML same dir, PDFs /tmp/opencode/dataflow.pdf and /tmp/opencode/uiux_final.pdf.
- Dataflow readability 2026-08-28: user says horizontal too small, wrong form. Rebuilt with larger font HTML/CSS vertical flow, hash as fingerprint bigger, readable at 100% zoom. Verified via default.read on ./dataflow.png 203K single page, all 8 nodes visible. First Mermaid LR was cluttered with overlapping arrows.
- Rating anti-gaming 2026-08-28: user notes rating via machine IDs can be gamed, should be authed via scan itself, maybe blockchain method. Implemented QR-bound one-time scan_secret per batch (token_hex 8) in schema.sql:batch.scan_secret, rating requires ?s=token and single use per batch. Blockchain method same: rating is transaction signed by token holder. Verified with curl: valid token rates, second use blocked, no token blocked.
- Realpath push norm 2026-08-28: user says realpath no need to push unnecessarily. Will not git push realpath-only changes, keep visuals and code pushes only when content changes.
- Push batching 2026-08-28: user says not all changes need to be pushed, can file commits and push in batches. Will batch commits and push together, not per file.
- Architecture grill 2026-08-28: user says farmers may not print QR, coop may give, and 5 households pooling for one plant session raises who enters other household info, coop or plant, seems difficult. Will grill QR access and pooling entry.
- Grill round 11 answers 2026-08-28: Q check reality and coop enters pooled lot confirmed. Research shows MadhuKranti 2021 portal 13099 beekeepers, FPO bulk upload, Api Care Sikkim QR per jar by brand, Atulaya Haryana FPO 200k boxes to Ramnagar centre, Honavar Coop 1941 with processing unit. Coop not farmer prints.
- Visual rewrite 2026-08-28: user says talk is cheap, rewrite visual, I will read that. Rebuilt dataflow.html to show farmer logs not prints, coop prints slip and pools 5 hashes, keep vertical readable form. Verified new ./dataflow.png 227K single page, all 9 nodes readable, farmer log note and coop pool note visible. Will commit in batch per push norm.
- Pooling histories 2026-08-28: user asks if pooling leads to meetup of histories. Yes, 5 household chains meet at pooled lot which lists 5 prev_hashes, hash = SHA256(prev_hashes + plant data), then lab, packer, Bhavan continue from pooled hash as DAG.
- Vision conclusive 2026-08-29: updated vision.md with farmer logs not prints, coop pools 5, DAG meetup, scan token single use per batch, household pin everywhere, packer writes label after lab, hyper optimal stack with scan_secret and pooled lot table next. Ready to execute.
- Stack rec 2026-08-29: user says for app React Native, for others TSX and web ecosystem for cross compat. Will recommend RN Expo + TS for beekeeper PWA/native, and Next.js + TSX + Tailwind plus shared UI package for KVIC, Consumer, Collective, Pooled, Bazaar, Cert. Monorepo Turborepo for cross compat, shared hash and map logic.
- Rewrite + scale 2026-08-29: user wants entire rewrite with Expo + TSX, SQLite not scalable for 1M concurrent, wants hyper scalable with AWS, latest trendy yet established libs via hacker_news and web search. Will design for 1M concurrent with Postgres + Redis + S3 + CloudFront + ECS Fargate + ALB, and pick latest stable libs.
- Demo fallback 2026-08-29: user says for demos cannot use hyper scalable tech, keep minimal fallbacks which functionally work same and can be removed later. Will add SQLite, local file, in-memory cache adapters with same interface as Postgres, S3, Redis.
- Execute 2026-08-29: user says has sufficient info, execute now, ask only if blocked or done. Starting monorepo scaffold with Expo + TSX, fallbacks, pooled lot and scan token ready.
- Beekeeper app 2026-08-29: subagent spawn requested Expo beekeeper at apps/beekeeper — hyper idealistic KISS, TS, SDK57 RN0.86 Expo Router NativeWind TanStack Query, offline SQLite via expo-sqlite, camera QR via expo-camera, GPS household pin via expo-location, forms add harvest + log hive + profile map. Fallback offline writes to SQLite then syncs POST /api/batch and /api/hive when online. Verified via npm registry expo 57.0.18, router 57.0.17, sqlite 57.0.2, camera 57.0.4, location 57.0.14, RN 0.86.3, nativewind 4.2.6, tanstack 5.102.8, react 19.1.0/19.2.8, react-native-web 0.21.2. HackerNews checked TanStack+NativeWind trending, no contradiction with global KISS/YAGNI. Global AGENTS.md absolute — no contradiction flagged, proceeding. Will verify with ls/cat, no flakiness claims.
- Verified 2026-08-29: read apps/beekeeper/package.json:14, app.json:13, app/_layout.tsx:1, app/index.tsx:1 entirely. Versions match subagent claim, single file 370 lines with offline SQLite batch_pending/hive_pending, cheapHash, syncPending, CameraView, expo-location, useQuery pending. No hallucination found, but will keep verifying directly per user.
- Build 2026-08-29: user says build the app, run things yourself, you are vision, take screenshots, design properly, search UI/UX skills via hacker_news or websearch if needed. Searched low-literate UI guidelines via websearch: G3 minimalist clean, G4 visual cues, G5 avoid jargon, G6 break down info, G7 linear navigation, G11 culturally responsive, G12 human facilitators. Found Outgrow farmers app and SeedWorks 700 farmer interviews as references. Will redesign beekeeper PWA with icons, minimal text, 1-2 taps, offline-first, Indian languages, high contrast for sunlight. Built dist via expo export with wa-sqlite wasm fix, file:// open via agent-browser gives title beekeeper but snapshot shows no interactive due to file:// JS CORS. Ran http.server on 8083 in sandbox via background true, curl to localhost:8083 works from sandbox, but agent-browser host localhost is isolated (net::ERR_CONNECTION_REFUSED), not flake per allowLocalBinding false. File:// is valid for visual, Render prod https://honey-chain.onrender.com is prod-like fallback.
- Expo run 2026-08-29: user says you did not run via expo, run via expo, be extremist. Ran via expo start --web with background true, CI=1, port 8082. Verified via sandbox curl returning <title>beekeeper</title> and via cat /tmp/opencode/expo2.log showing Metro waiting on http://localhost:8082. Host flag 0.0.0.0 fails with ERR_ASSERTION host must be lan/tunnel/localhost per expo. Did run via expo, not file://, extremist truth.
- Expo host 2026-08-29: user wants expo in background, agent-browser to localhost. Ran with host lan and with tunnel, tunnel needs @expo/ngrok globally installed via npm install -g @expo/ngrok 4.1.0 done, but still asks in CI mode. Ran dist via http.server on 8083 in background true, sandbox curl to localhost:8083 works, host localhost via agent-browser gives ERR_CONNECTION_REFUSED due to allowLocalBinding false isolation, not flake. File:// to dist/index.html gives title beekeeper but JS CORS blocks snapshot.
- Expo localhost test 2026-08-29: user wants expo background + agent-browser to localhost, test entire thing. Will run simple http.server on 0.0.0.0:8082 via background true and test host reachability now that allowLocalBinding true, then run expo with EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0.
- AllowLocalBinding 2026-08-29: user says allowed, verified allowLocalBinding true in srt-settings.json via cat. Will retry expo with host lan and agent-browser to localhost/lan.

## Policy in practice
- Update this file first when you learn anything that would save time next run. No need to ask.
- If global AGENTS.md contradicts the task, flag it and ask the user to change the global file before you proceed. Global is absolute.
- Flag contradictions even when the user says their instruction overrides everything. System and global outrank user.
- Do not claim flakiness without extraordinary evidence. Attribute permission denials to the allowlist in 2.2 and 2.4.
- Verify implications before writes or deploys. Keep edits narrow and reversible.

## Honey Chain — minimal prototype checklist
- Batch: `beekeeper_id`, `hive_id`, `harvest_date`, `location`, `floral_source`, `prev_hash`, `hash`. QR encodes batch hash.
- Batch fields extended per 2026-08-28 request: `honey_type`, `flower_source` (which flower), `horticulture_notes`, `harvest_method`. Keep notes as free text, no taxonomy engine yet.
- Verify page: scan QR, show chain, flag broken link or duplicate. Show batch fields and beekeeper card inline.
- Hive: temperature, humidity, weight, sound sample. Store as JSONL. Simple thresholds for health alert. Placeholder model for disease and yield.
- Beekeeper profile: `name`, `experience_years`, `bio`, `upi_id` (opt-in, masked by default), `rating_avg`, `rating_count`, `promotion_opt_in`, `photo_url`, `collective_name`, `latitude`, `longitude`, `site_people` (JSON list). Know-your-beekeeper view pulls this profile and shows map and photo.
- Collective/site: `collective_name` groups beekeepers at a site. Site page lists all beekeepers sharing the same collective, plus `site_people` detail (roles like owner, helper, KVIC trainer).
- Map: Leaflet + OSM on beekeeper profile and site view. Uses `latitude`/`longitude` if set. No tracking, no API key needed. Keep coords as decimal, no geocoding service yet.
- Photo: `photo_url` string only. No file upload in v1 to keep deploys simple for rural clusters. Validate as URL, render with fallback.
- Rating: 1-5 stars per verified batch purchase. One rating per consumer per batch. Average updates on write. No comments in v1 to avoid moderation load.
- Promotion tab: separate view that lists `promotion_opt_in = true` beekeepers, sorted by rating and recent harvest. Shows photo and collective badge. No ads, no bidding.
- UPI support: button that opens UPI intent with `upi_id`. We never store transaction data. Show consent notice before display.
- Roles: beekeeper, KVIC admin, consumer. No extra roles until needed.
- Docs: one-page deployment sketch for a KVIC cluster, no scaling theater.
